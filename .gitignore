# Secrets
.env
.env.local
.env.*.local
**/.env

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/
contracts/.gas-snapshot

# Node
node_modules/
*.log

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# Private working files
private/
docs/prompts/
docs/strategy-*.md

# Local editor / agent session state
.claude/

# video build artifacts (committed via gh release instead)
video/demo.mp4
video/demoTEMP*
video/audio_sent/
video/audio/silence/
video/audio/narration_*.mp3
video/voice_test_*.mp3
video/test_voices.py
demoTEMP_MPY_wvf_snd.mp4
# Regenerable from generate_*.py scripts — keep scripts + final mp4 only
video/audio_addendum/
video/audio_sent_addendum/
video/audio_codebase/
video/audio_sent_codebase/
video/slides_addendum/
video/slides_codebase/

# Aster API docs (large, fetched from upstream for reference only)
aster-futures-testnet.md

# Demo wallet keys generated at runtime (testnet only, but still — don't commit)
underwriter/*-wallet.txt
